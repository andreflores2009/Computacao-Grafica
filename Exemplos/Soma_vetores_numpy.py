import numpy as np

# Definindo os vetores como arrays NumPy
vetor_a = np.array([2, 1])
vetor_b = np.array([-3, 3])

# Somando os vetores
resultado = np.add(vetor_a, vetor_b)
print(f"Soma dos vetores: {resultado}")
